Create notebook instance of type ml.m5.xlarge for optimal experience,
upload final_model.joblib, and inference.py then run the endpoint code in the notebook, 
and it will create a model.tar.gz file in the s3 bucket and create a working endpoint for the model.
